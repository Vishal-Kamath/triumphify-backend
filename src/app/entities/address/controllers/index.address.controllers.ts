import handleCreateAddress from "./create.address.controller";
import handleGetAllAddresses from "./get.all.address.controller";
import handleDeleteAddress from "./delete.address.controller";
import handleAddressGetById from "./get.byId.controller";
import handleUpdateAddress from "./update.address.controller";

export default {
  handleCreateAddress,
  handleGetAllAddresses,
  handleDeleteAddress,
  handleAddressGetById,
  handleUpdateAddress,
};
